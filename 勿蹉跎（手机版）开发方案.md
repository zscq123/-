# 勿蹉跎（手机版）开发方案

> **轻量级移动端任务管理与番茄钟应用**  
> 版本：v1.0  
> 制定日期：2025-11-23

---

## 📋 目录

- [项目概述](#项目概述)
- [核心功能](#核心功能)
- [技术方案](#技术方案)
- [UI/UX设计](#uiux设计)
- [悬浮窗改造方案](#悬浮窗改造方案)
- [开发路线图](#开发路线图)
- [技术实现](#技术实现)
- [部署上线](#部署上线)

---

## 🎯 项目概述

### 项目背景
基于桌面版「勿蹉跎」应用的成功经验，将核心功能移植到移动端，让用户随时随地管理任务、保持专注。

### 设计理念
- 📱 **移动优先** - 专为触摸屏优化
- ⚡ **轻量简洁** - 只保留核心必备功能  
- 🎨 **直观易用** - 单手操作，快速上手
- 🔋 **省电高效** - 后台占用最小化

### 目标用户
- 需要移动端管理任务的上班族
- 学生群体（课间/路上记录任务）
- 需要番茄钟提升专注力的用户
- 桌面版用户的移动端延伸

---

## ✨ 核心功能

### 必备功能（精简版）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| **任务管理** | 增删改查、标记完成 | ⭐⭐⭐ |
| **任务分类** | 工作/生活/学习/健康 4种 | ⭐⭐⭐ |
| **分类筛选** | 快速切换查看不同分类 | ⭐⭐⭐ |
| **番茄钟** | 25分钟专注计时器 | ⭐⭐⭐ |
| **今日统计** | 完成数、完成率 | ⭐⭐ |
| **本地存储** | 数据持久化保存 | ⭐⭐⭐ |
| **暗色模式** | 夜间友好主题 | ⭐⭐ |

### 移动端特色增强功能（第二版）

**设计理念：** 充分利用移动端特性，打造有趣、温暖、高效的使用体验。

| 功能 | 说明 | 优先级 | 开发时间 |
|------|------|--------|---------|
| **摇一摇随机任务** | 摇动手机随机选择一个任务 | ⭐⭐⭐ | 0.5天 |
| **连续打卡成就** | 连续完成任务获得徽章奖励 | ⭐⭐⭐ | 1天 |
| **专注模式** | 番茄钟期间屏蔽干扰、播放白噪音 | ⭐⭐⭐ | 1天 |
| **语音快速添加** | 长按FAB按钮，语音识别添加任务 | ⭐⭐ | 0.5天 |
| **每日激励语** | 每日开屏显示激励名言 | ⭐⭐ | 0.3天 |
| **时间胶囊** | 每周自动生成成就回顾 | ⭐⭐ | 0.5天 |

**总计开发时间：3.8天**

**功能价值：**
```
✅ 移动端独有 - 摇一摇、语音识别、重力感应
✅ 游戏化设计 - 打卡、成就、徽章激励
✅ 情感连接 - 每日激励、时间胶囊
✅ 专注增强 - 白噪音、沉浸模式
✅ 提升粘性 - 让用户真正爱上使用
```

### 暂不实现功能

```
❌ 周期任务（每日/每周/每月重置）
❌ 详细统计图表
❌ 截止时间提醒
❌ 云同步
❌ 任务描述编辑
❌ 复杂的设置面板

理由：移动端第一版专注核心体验，避免功能臃肿
```

---

## 🔧 技术方案

### 方案选择：PWA（渐进式Web应用）⭐

**为什么选择PWA？**
- ✅ 基于现有代码，改动最小（复用70%代码）
- ✅ 一次开发，iOS+Android都能用
- ✅ 无需应用商店审核，快速迭代
- ✅ 开发周期短（3-5天）
- ✅ 可安装到手机桌面
- ✅ 离线可用

### 技术栈
```
核心技术：
├── HTML5 + CSS3 (UI界面)
├── Vanilla JavaScript (业务逻辑)
├── Service Worker (离线缓存)
├── LocalStorage (数据存储)
├── Web Notifications API (通知)
└── Vibration API (振动反馈)
```

---

## 🎨 UI/UX设计

### 主界面布局

```
┌─────────────────────────────┐
│ 📝 勿蹉跎    [🍅] [📊] [⚙️] │ ← 顶栏
├─────────────────────────────┤
│ 🔍 搜索任务...              │ 
├─────────────────────────────┤
│ 🔵 🟢 🟡 🔴  全部           │ ← 分类筛选
├─────────────────────────────┤
│  ┌──────────────────────┐  │
│  │🔵 完成项目文档    🍅 │  │ ← 任务卡片
│  │今天              ✓  │  │
│  └──────────────────────┘  │
│                              │
│  ┌──────────────────────┐  │
│  │🟢 买菜            🍅 │  │
│  │                   ✓  │  │
│  └──────────────────────┘  │
├─────────────────────────────┤
│   今日: 2/5  🍅×3           │ ← 底部状态栏
└─────────────────────────────┘
          ↑
       [➕] FAB快速添加
```

### 番茄钟全屏界面

```
┌─────────────────────────────┐
│         ✕ 退出              │
├─────────────────────────────┤
│     完成项目文档             │
│                              │
│        25:00                │ ← 大号计时
│                              │
│      [开始专注]             │
│                              │
│   今日 🍅×3  专注 75分钟    │
└─────────────────────────────┘
```

### 交互设计

**触摸手势：**
- 向左滑动 → 快速删除
- 向右滑动 → 标记完成
- 长按 → 显示更多选项
- 下拉刷新 → 刷新任务列表

**按钮尺寸规范：**
- 最小点击区域：48x48px
- 按钮高度：48-56px
- 图标大小：24-32px
- 文字大小：16-18px

---

## 📱 悬浮窗改造方案

### 方案A：Android悬浮球（需原生开发）

```
┌──────────┐
│    🍅    │  ← 小悬浮球
└──────────┘
     ↓ 点击展开
┌──────────────┐
│ 📝 快速添加  │
│ ⏱️ 开始番茄钟│
│ ✅ 查看任务  │
└──────────────┘
```

### 方案B：Widget桌面小组件 ⭐推荐

```
┌────────────────────────┐
│ 📝 勿蹉跎              │
│ ────────────────────   │
│ 🔵 完成项目文档    ⏱️  │
│ 🟢 买菜             ✓  │
│ 🟡 学习React        ⏱️  │
│ ────────────────────   │
│ 今日: 2/5  🍅×3        │
└────────────────────────┘

交互：
- 点击任务 → 打开应用
- 点击 ⏱️ → 启动番茄钟
- 点击 ✓ → 标记完成
```

### 方案C：通知栏常驻（PWA可实现）✅

```
番茄钟运行时显示：
┌────────────────────────┐
│ 🍅 勿蹉跎 - 专注中     │
│ 25:00                  │
│ 任务：完成项目文档     │
│ [暂停] [结束] [+5分钟] │
└────────────────────────┘
```

---

## 🗓️ 开发路线图

### 第一阶段：MVP核心功能（3天）

**Day 1：基础框架**
- ✅ 创建PWA项目结构
- ✅ 配置manifest.json
- ✅ 移动端响应式布局
- ✅ 任务列表UI
- ✅ 底部状态栏
- ✅ FAB快速添加按钮

**Day 2：核心功能**
- ✅ 任务CRUD功能
- ✅ LocalStorage数据持久化
- ✅ 任务分类与筛选
- ✅ 番茄钟计时器
- ✅ 触摸手势（左滑删除、右滑完成）

**Day 3：优化发布**
- ✅ 今日统计功能
- ✅ 暗色模式
- ✅ Service Worker（离线支持）
- ✅ 性能优化
- ✅ 真机测试
- ✅ 部署上线

### 第二阶段：移动端特色功能（4天）

**Day 4：游戏化激励**
- ✅ 连续打卡成就系统（1天）
  - 连续天数统计
  - 徽章解锁动画
  - 主界面显示连续天数
- ✅ 每日激励语（0.3天）
  - 随机名言库
  - 开屏显示动画

**Day 5：移动端交互**
- ✅ 摇一摇随机任务（0.5天）
  - 加速度传感器检测
  - 随机抽取动画
- ✅ 语音快速添加（0.5天）
  - 语音识别API
  - 智能分类解析

**Day 6：专注增强**
- ✅ 专注模式（1天）
  - 屏幕常亮
  - 白噪音播放
  - 全屏沉浸
- ✅ 时间胶囊（0.5天）
  - 每周数据统计
  - 成就回顾卡片

**Day 7：优化与测试**
- 性能优化
- 动画效果调优
- 真机测试各项增强功能

---

## 💻 技术实现

### 项目结构

```
mobile/
├── index.html              # 主页面
├── manifest.json           # PWA配置
├── sw.js                  # Service Worker
├── vercel.json            # Vercel部署配置
├── capacitor.config.json  # Capacitor配置（APK打包）
├── css/
│   ├── base.css           # 基础样式
│   ├── mobile.css         # 移动端适配
│   ├── dark.css           # 暗色主题
│   └── animations.css     # 动画效果
├── js/
│   ├── app.js             # 主逻辑
│   ├── task.js            # 任务管理
│   ├── pomodoro.js        # 番茄钟
│   ├── storage.js         # 本地存储
│   ├── shake.js           # 摇一摇功能
│   ├── voice.js           # 语音识别
│   ├── achievement.js     # 成就系统
│   ├── focus.js           # 专注模式
│   └── motivation.js      # 激励语&时间胶囊
├── assets/
│   ├── icons/             # 应用图标
│   └── sounds/            # 白噪音音效
│       ├── rain.mp3
│       ├── forest.mp3
│       ├── ocean.mp3
│       └── cafe.mp3
└── data/
    └── quotes.json        # 激励语库
```

### manifest.json配置

```json
{
  "name": "勿蹉跎 - 专注Todo",
  "short_name": "勿蹉跎",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#2196f3",
  "orientation": "portrait",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### 核心代码示例

```javascript
// 滑动手势实现
class SwipeDetector {
  constructor(element, onLeft, onRight) {
    let startX = 0;
    element.addEventListener('touchstart', e => {
      startX = e.touches[0].clientX;
    });
    element.addEventListener('touchend', e => {
      const endX = e.changedTouches[0].clientX;
      const distance = endX - startX;
      
      if (Math.abs(distance) > 100) {
        distance < 0 ? onLeft?.() : onRight?.();
      }
    });
  }
}

// 番茄钟计时器
class PomodoroTimer {
  async start() {
    // 请求屏幕常亮
    if ('wakeLock' in navigator) {
      this.wakeLock = await navigator.wakeLock.request('screen');
    }
    
    // 启动计时
    let seconds = 25 * 60;
    this.interval = setInterval(() => {
      seconds--;
      this.updateDisplay(seconds);
      if (seconds <= 0) this.complete();
    }, 1000);
  }
  
  complete() {
    // 振动反馈
    navigator.vibrate?.([200, 100, 200]);
    
    // 通知
    new Notification('🎉 番茄钟完成！', {
      body: '休息5分钟吧~'
    });
  }
}
```

---

## 🎮 增强功能技术实现

### 1. 摇一摇随机任务

**功能说明：** 当任务太多不知道从哪个开始时，摇动手机随机抽取一个任务。

**技术实现：**
```javascript
// 使用DeviceMotion API检测摇动
class ShakeDetector {
  constructor(onShake) {
    this.lastTime = 0;
    this.lastX = 0;
    this.lastY = 0;
    this.lastZ = 0;
    this.shakeThreshold = 15; // 摇动阈值
    
    window.addEventListener('devicemotion', (e) => {
      const current = e.accelerationIncludingGravity;
      const currentTime = new Date().getTime();
      
      if ((currentTime - this.lastTime) > 100) {
        const diffTime = currentTime - this.lastTime;
        const diffX = Math.abs(current.x - this.lastX);
        const diffY = Math.abs(current.y - this.lastY);
        const diffZ = Math.abs(current.z - this.lastZ);
        
        const speed = (diffX + diffY + diffZ) / diffTime * 10000;
        
        if (speed > this.shakeThreshold) {
          onShake?.();
        }
        
        this.lastTime = currentTime;
        this.lastX = current.x;
        this.lastY = current.y;
        this.lastZ = current.z;
      }
    });
  }
}

// 使用示例
const shaker = new ShakeDetector(() => {
  const incompleteTasks = tasks.filter(t => !t.completed);
  if (incompleteTasks.length > 0) {
    const randomTask = incompleteTasks[Math.floor(Math.random() * incompleteTasks.length)];
    
    // 显示动画效果
    showRandomTaskModal(randomTask);
    
    // 振动反馈
    navigator.vibrate?.([100, 50, 100]);
  }
});

// 显示随机任务的弹窗
function showRandomTaskModal(task) {
  const modal = document.createElement('div');
  modal.className = 'random-task-modal';
  modal.innerHTML = `
    <div class="modal-content">
      <div class="dice-animation">🎲</div>
      <h2>就决定是你了！</h2>
      <div class="task-card">
        ${task.icon} ${task.title}
      </div>
      <button onclick="this.closest('.random-task-modal').remove()">
        开始这个任务
      </button>
    </div>
  `;
  document.body.appendChild(modal);
}
```

**CSS动画：**
```css
.random-task-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0,0,0,0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  animation: fadeIn 0.3s;
}

.dice-animation {
  font-size: 80px;
  animation: spin 0.6s ease-out;
}

@keyframes spin {
  from { transform: rotate(0deg) scale(0.5); }
  to { transform: rotate(360deg) scale(1); }
}
```

---

### 2. 连续打卡成就系统

**功能说明：** 连续完成任务获得徽章，提升用户粘性。

**数据结构：**
```javascript
const achievements = {
  streak: 0,           // 连续天数
  lastCheckDate: null, // 最后打卡日期
  totalDays: 0,        // 总打卡天数
  badges: [],          // 已获得徽章
  
  // 徽章定义
  badgeList: [
    { id: 'bronze', name: '铜牌', days: 3, icon: '🥉' },
    { id: 'silver', name: '银牌', days: 7, icon: '🥈' },
    { id: 'gold', name: '金牌', days: 30, icon: '🥇' },
    { id: 'diamond', name: '钻石', days: 100, icon: '💎' }
  ]
};

// 检查打卡
function checkDailyStreak() {
  const today = new Date().toDateString();
  const lastDate = achievements.lastCheckDate;
  
  if (lastDate === today) {
    return; // 今天已打卡
  }
  
  const yesterday = new Date();
  yesterday.setDate(yesterday.getDate() - 1);
  
  if (lastDate === yesterday.toDateString()) {
    // 连续打卡
    achievements.streak++;
  } else if (lastDate !== today) {
    // 中断了，重置
    achievements.streak = 1;
  }
  
  achievements.lastCheckDate = today;
  achievements.totalDays++;
  
  // 检查是否获得新徽章
  checkNewBadge();
  
  saveAchievements();
}

// 检查新徽章
function checkNewBadge() {
  achievements.badgeList.forEach(badge => {
    if (achievements.streak >= badge.days && 
        !achievements.badges.includes(badge.id)) {
      // 获得新徽章！
      achievements.badges.push(badge.id);
      showBadgeUnlockAnimation(badge);
    }
  });
}

// 显示徽章解锁动画
function showBadgeUnlockAnimation(badge) {
  const modal = document.createElement('div');
  modal.className = 'badge-unlock-modal';
  modal.innerHTML = `
    <div class="modal-content">
      <div class="badge-icon">${badge.icon}</div>
      <h2>🎉 成就解锁！</h2>
      <p>连续打卡 ${badge.days} 天</p>
      <p class="badge-name">${badge.name}</p>
      <button onclick="this.closest('.badge-unlock-modal').remove()">
        太棒了！
      </button>
    </div>
  `;
  document.body.appendChild(modal);
  
  // 振动庆祝
  navigator.vibrate?.([200, 100, 200, 100, 200]);
}

// 在主界面显示打卡状态
function renderStreakStatus() {
  return `
    <div class="streak-widget">
      <span class="fire-icon">🔥</span>
      <span class="streak-number">${achievements.streak}</span>
      <span class="streak-text">天连续打卡</span>
    </div>
  `;
}
```

---

### 3. 专注模式

**功能说明：** 番茄钟期间屏蔽干扰、可选播放白噪音。

**技术实现：**
```javascript
class FocusMode {
  constructor() {
    this.isActive = false;
    this.audio = null;
    this.whitenoise = [
      { name: '雨声', url: '/sounds/rain.mp3', icon: '🌧️' },
      { name: '森林', url: '/sounds/forest.mp3', icon: '🌲' },
      { name: '海浪', url: '/sounds/ocean.mp3', icon: '🌊' },
      { name: '咖啡厅', url: '/sounds/cafe.mp3', icon: '☕' }
    ];
  }
  
  async start(soundIndex = null) {
    this.isActive = true;
    
    // 1. 请求屏幕常亮
    if ('wakeLock' in navigator) {
      this.wakeLock = await navigator.wakeLock.request('screen');
    }
    
    // 2. 全屏模式（可选）
    if (document.documentElement.requestFullscreen) {
      document.documentElement.requestFullscreen().catch(() => {});
    }
    
    // 3. 播放白噪音
    if (soundIndex !== null) {
      this.playWhiteNoise(soundIndex);
    }
    
    // 4. 显示专注界面
    this.showFocusUI();
    
    // 5. 阻止通知（PWA限制，仅提示用户手动开启勿扰模式）
    this.showDoNotDisturbTip();
  }
  
  playWhiteNoise(index) {
    const sound = this.whitenoise[index];
    this.audio = new Audio(sound.url);
    this.audio.loop = true;
    this.audio.volume = 0.3;
    this.audio.play();
  }
  
  stop() {
    this.isActive = false;
    
    // 停止白噪音
    if (this.audio) {
      this.audio.pause();
      this.audio = null;
    }
    
    // 释放屏幕常亮
    if (this.wakeLock) {
      this.wakeLock.release();
    }
    
    // 退出全屏
    if (document.exitFullscreen) {
      document.exitFullscreen().catch(() => {});
    }
  }
  
  showFocusUI() {
    document.body.classList.add('focus-mode');
    // 隐藏其他任务，只显示当前任务和计时器
    document.querySelector('.task-list').style.display = 'none';
    document.querySelector('.focus-overlay').style.display = 'flex';
  }
  
  showDoNotDisturbTip() {
    // 提示用户开启手机勿扰模式
    setTimeout(() => {
      showToast('💡 建议开启手机勿扰模式，专注效果更佳');
    }, 2000);
  }
}

// 白噪音选择界面
function showWhiteNoiseSelector() {
  const modal = document.createElement('div');
  modal.className = 'whitenoise-modal';
  modal.innerHTML = `
    <div class="modal-content">
      <h3>选择专注音效</h3>
      <div class="sound-list">
        ${focusMode.whitenoise.map((sound, i) => `
          <button class="sound-item" onclick="selectSound(${i})">
            <span class="icon">${sound.icon}</span>
            <span class="name">${sound.name}</span>
          </button>
        `).join('')}
        <button class="sound-item" onclick="selectSound(null)">
          <span class="icon">🔇</span>
          <span class="name">静音</span>
        </button>
      </div>
    </div>
  `;
  document.body.appendChild(modal);
}
```

**专注模式样式：**
```css
.focus-mode {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.focus-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: none;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: white;
  text-align: center;
  padding: 20px;
}

.focus-timer {
  font-size: 72px;
  font-weight: bold;
  margin: 40px 0;
  text-shadow: 0 4px 20px rgba(0,0,0,0.3);
}
```

---

### 4. 语音快速添加

**功能说明：** 长按FAB按钮，通过语音识别快速添加任务。

**技术实现：**
```javascript
class VoiceInput {
  constructor() {
    // 检查浏览器支持
    const SpeechRecognition = window.SpeechRecognition || 
                             window.webkitSpeechRecognition;
    
    if (!SpeechRecognition) {
      console.warn('该浏览器不支持语音识别');
      return;
    }
    
    this.recognition = new SpeechRecognition();
    this.recognition.lang = 'zh-CN';
    this.recognition.continuous = false;
    this.recognition.interimResults = false;
    
    this.setupListeners();
  }
  
  setupListeners() {
    this.recognition.onresult = (event) => {
      const transcript = event.results[0][0].transcript;
      this.handleVoiceInput(transcript);
    };
    
    this.recognition.onerror = (event) => {
      console.error('语音识别错误:', event.error);
      showToast('❌ 识别失败，请重试');
    };
    
    this.recognition.onend = () => {
      this.hideListeningUI();
    };
  }
  
  start() {
    this.showListeningUI();
    this.recognition.start();
    navigator.vibrate?.([50]); // 轻微振动反馈
  }
  
  handleVoiceInput(text) {
    // 智能解析语音内容
    const task = this.parseVoiceCommand(text);
    
    // 添加任务
    addTask(task);
    
    // 显示确认
    showToast(`✅ 已添加: ${task.title}`);
    navigator.vibrate?.([100]);
  }
  
  parseVoiceCommand(text) {
    // 简单的智能解析
    let category = '🔵'; // 默认工作
    
    if (text.includes('买') || text.includes('购物')) {
      category = '🟢'; // 生活
    } else if (text.includes('学习') || text.includes('看书') || text.includes('课程')) {
      category = '🟡'; // 学习
    } else if (text.includes('运动') || text.includes('锻炼') || text.includes('健身')) {
      category = '🔴'; // 健康
    }
    
    return {
      id: Date.now(),
      title: text,
      category: category,
      completed: false,
      createdAt: new Date().toISOString()
    };
  }
  
  showListeningUI() {
    const overlay = document.createElement('div');
    overlay.id = 'voice-overlay';
    overlay.innerHTML = `
      <div class="voice-content">
        <div class="voice-icon">🎤</div>
        <div class="voice-wave">
          <span></span><span></span><span></span>
        </div>
        <p>请说出要添加的任务</p>
        <button onclick="voiceInput.recognition.stop()">取消</button>
      </div>
    `;
    document.body.appendChild(overlay);
  }
  
  hideListeningUI() {
    document.getElementById('voice-overlay')?.remove();
  }
}

// 初始化语音输入
const voiceInput = new VoiceInput();

// FAB按钮长按监听
const fabButton = document.getElementById('fab-add');
let pressTimer;

fabButton.addEventListener('touchstart', () => {
  pressTimer = setTimeout(() => {
    voiceInput.start();
  }, 500); // 长按500ms触发
});

fabButton.addEventListener('touchend', () => {
  clearTimeout(pressTimer);
});
```

**语音识别动画：**
```css
.voice-wave {
  display: flex;
  gap: 8px;
  justify-content: center;
  margin: 20px 0;
}

.voice-wave span {
  width: 4px;
  height: 40px;
  background: #2196f3;
  border-radius: 4px;
  animation: wave 1s ease-in-out infinite;
}

.voice-wave span:nth-child(2) {
  animation-delay: 0.2s;
}

.voice-wave span:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes wave {
  0%, 100% { height: 20px; }
  50% { height: 60px; }
}
```

---

### 5. 每日激励语 + 时间胶囊

**功能说明：** 每日开屏显示激励名言，每周生成成就回顾。

**技术实现：**
```javascript
// 激励语库
const motivationalQuotes = [
  { text: '成功就是将简单的事情重复做', author: '杰克·韦尔奇' },
  { text: '今天的你比昨天更进一步', author: '村上春树' },
  { text: '专注当下，活在今天', author: '埃克哈特·托利' },
  { text: '要么出众，要么出局', author: '奥格·曼狄诺' },
  { text: '成功的秘诀就是每天进步一点点', author: '稻盛和夫' },
  { text: '你的时间有限，不要浪费在别人的生活里', author: '史蒂夫·乔布斯' },
  { text: '行动是治愈恐惧的良药', author: '威廉·詹姆斯' },
  { text: '不要等待机会，而要创造机会', author: '乔治·伯纳德·肖' }
];

// 每日激励语
class DailyMotivation {
  show() {
    const today = new Date().toDateString();
    const lastShown = localStorage.getItem('lastMotivationDate');
    
    // 每天只显示一次
    if (lastShown === today) return;
    
    const quote = this.getTodayQuote();
    this.showModal(quote);
    
    localStorage.setItem('lastMotivationDate', today);
  }
  
  getTodayQuote() {
    // 根据日期选择固定的名言（同一天显示相同内容）
    const dayOfYear = Math.floor((new Date() - new Date(new Date().getFullYear(), 0, 0)) / 86400000);
    const index = dayOfYear % motivationalQuotes.length;
    return motivationalQuotes[index];
  }
  
  showModal(quote) {
    const modal = document.createElement('div');
    modal.className = 'daily-motivation-modal';
    modal.innerHTML = `
      <div class="modal-content">
        <div class="quote-icon">✨</div>
        <blockquote class="quote-text">"${quote.text}"</blockquote>
        <p class="quote-author">— ${quote.author}</p>
        <button onclick="this.closest('.daily-motivation-modal').remove()">
          开始新的一天
        </button>
      </div>
    `;
    document.body.appendChild(modal);
  }
}

// 时间胶囊（每周回顾）
class TimeCapsule {
  generateWeeklyReview() {
    const endDate = new Date();
    const startDate = new Date();
    startDate.setDate(startDate.getDate() - 7);
    
    // 统计本周数据
    const stats = this.getWeeklyStats(startDate, endDate);
    
    // 生成回顾卡片
    this.showReviewModal(stats);
  }
  
  getWeeklyStats(startDate, endDate) {
    const tasks = JSON.parse(localStorage.getItem('tasks') || '[]');
    const completedTasks = tasks.filter(t => {
      const taskDate = new Date(t.completedAt);
      return t.completed && taskDate >= startDate && taskDate <= endDate;
    });
    
    const pomodoroHistory = JSON.parse(localStorage.getItem('pomodoroHistory') || '[]');
    const weeklyPomodoros = pomodoroHistory.filter(p => {
      const pomodoroDate = new Date(p.date);
      return pomodoroDate >= startDate && pomodoroDate <= endDate;
    });
    
    return {
      completedCount: completedTasks.length,
      pomodoroCount: weeklyPomodoros.length,
      focusMinutes: weeklyPomodoros.length * 25,
      topCategory: this.getTopCategory(completedTasks),
      achievement: this.getWeeklyAchievement(completedTasks.length)
    };
  }
  
  getTopCategory(tasks) {
    const categories = {};
    tasks.forEach(t => {
      categories[t.category] = (categories[t.category] || 0) + 1;
    });
    
    const categoryNames = {
      '🔵': '工作',
      '🟢': '生活',
      '🟡': '学习',
      '🔴': '健康'
    };
    
    const topCat = Object.keys(categories).sort((a, b) => 
      categories[b] - categories[a]
    )[0];
    
    return categoryNames[topCat] || '工作';
  }
  
  getWeeklyAchievement(count) {
    if (count >= 30) return { icon: '🏆', text: '超级高产' };
    if (count >= 20) return { icon: '🌟', text: '效率达人' };
    if (count >= 10) return { icon: '👍', text: '稳步前进' };
    return { icon: '🌱', text: '持续成长' };
  }
  
  showReviewModal(stats) {
    const modal = document.createElement('div');
    modal.className = 'time-capsule-modal';
    modal.innerHTML = `
      <div class="modal-content">
        <h2>📦 本周时间胶囊</h2>
        <div class="review-stats">
          <div class="stat-item">
            <div class="stat-number">${stats.completedCount}</div>
            <div class="stat-label">完成任务</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">${stats.pomodoroCount}</div>
            <div class="stat-label">🍅 番茄钟</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">${stats.focusMinutes}</div>
            <div class="stat-label">专注分钟</div>
          </div>
        </div>
        <div class="achievement-badge">
          <span class="badge-icon">${stats.achievement.icon}</span>
          <span class="badge-text">${stats.achievement.text}</span>
        </div>
        <p class="review-text">
          本周你主要专注于<strong>${stats.topCategory}</strong>领域，
          继续保持这份热情！
        </p>
        <div class="share-buttons">
          <button onclick="shareCapsule(${JSON.stringify(stats).replace(/"/g, '&quot;')})">
            📤 分享成就
          </button>
          <button onclick="this.closest('.time-capsule-modal').remove()">
            关闭
          </button>
        </div>
      </div>
    `;
    document.body.appendChild(modal);
  }
  
  // 自动检查是否到了周日晚上
  autoCheck() {
    const now = new Date();
    const day = now.getDay();
    const hour = now.getHours();
    
    // 每周日晚上8点触发
    if (day === 0 && hour === 20) {
      const lastReview = localStorage.getItem('lastWeeklyReview');
      const today = now.toDateString();
      
      if (lastReview !== today) {
        this.generateWeeklyReview();
        localStorage.setItem('lastWeeklyReview', today);
      }
    }
  }
}

// 初始化
const dailyMotivation = new DailyMotivation();
const timeCapsule = new TimeCapsule();

// 应用启动时显示每日激励
window.addEventListener('load', () => {
  setTimeout(() => {
    dailyMotivation.show();
  }, 500);
});

// 每小时检查一次是否需要显示周回顾
setInterval(() => {
  timeCapsule.autoCheck();
}, 3600000); // 1小时
```

**时间胶囊样式：**
```css
.time-capsule-modal {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.review-stats {
  display: flex;
  justify-content: space-around;
  margin: 30px 0;
}

.stat-item {
  text-align: center;
}

.stat-number {
  font-size: 36px;
  font-weight: bold;
  margin-bottom: 8px;
}

.achievement-badge {
  background: rgba(255,255,255,0.2);
  padding: 16px;
  border-radius: 12px;
  text-align: center;
  margin: 20px 0;
}

.badge-icon {
  font-size: 48px;
}
```

---

## 🚀 部署上线方案

### 🎯 双轨部署策略（个人使用最佳实践）

```
主要方案：云端PWA (Vercel)  ←  日常使用
    ↓
备用方案：本地APK打包      ←  离线备份
```

---

### 方案1️⃣：云端PWA部署（主要使用）⭐⭐⭐⭐⭐

**优势：**
- ✅ 永久免费托管
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 随时更新，无需重装
- ✅ 多设备同步访问

**详细步骤：**

#### Step 1：准备部署
```bash
# 1. 安装Vercel CLI
npm i -g vercel

# 2. 登录Vercel（使用GitHub账号）
vercel login

# 3. 在项目根目录创建 vercel.json
{
  "buildCommand": null,
  "outputDirectory": "./",
  "framework": null
}
```

#### Step 2：部署到Vercel
```bash
# 进入项目目录
cd "c:\Users\sjc12\Desktop\程序开发\勿蹉跎（手机版）"

# 首次部署
vercel

# 生产环境部署
vercel --prod

# 获得永久链接
✅ https://wucuoduo-你的用户名.vercel.app
```

#### Step 3：手机安装PWA

**iOS设备（Safari）：**
```
1. 用Safari访问你的Vercel链接
2. 点击底部 [分享] 按钮 (□↑)
3. 滚动找到 [添加到主屏幕]
4. 编辑名称为"勿蹉跎"
5. 点击 [添加]
6. ✅ 主屏幕出现App图标！
```

**Android设备（Chrome）：**
```
1. 用Chrome访问你的Vercel链接
2. 点击右上角菜单 [⋮]
3. 点击 [安装应用] 或 [添加到主屏幕]
4. 点击 [安装]
5. ✅ 桌面出现App图标！
```

#### Step 4：数据备份机制
```javascript
// 在app.js中添加导出功能
function setupBackup() {
  // 每天自动备份到浏览器下载
  setInterval(() => {
    const data = {
      tasks: JSON.parse(localStorage.getItem('tasks') || '[]'),
      stats: JSON.parse(localStorage.getItem('stats') || '{}'),
      backup_time: new Date().toISOString()
    };
    
    const blob = new Blob([JSON.stringify(data, null, 2)], 
                          {type: 'application/json'});
    const url = URL.createObjectURL(blob);
    
    // 手动点击导出按钮时下载
    document.getElementById('export-btn').onclick = () => {
      const a = document.createElement('a');
      a.href = url;
      a.download = `勿蹉跎备份_${new Date().toLocaleDateString()}.json`;
      a.click();
    };
  }, 86400000); // 24小时
}
```

---

### 方案4️⃣：Android APK打包（备用离线）⭐⭐⭐⭐

**使用场景：**
- 完全离线使用
- 不依赖网络
- 拥有原生App体验
- 可访问更多系统权限

**方案A：PWA Builder（最简单）**

```bash
# 1. 先部署PWA到Vercel（方案1）
# 2. 访问 https://www.pwabuilder.com
# 3. 输入你的PWA链接
# 4. 点击 [Package For Stores]
# 5. 选择 [Android] → [Generate]
# 6. 下载APK文件
# 7. 传输到手机安装

优点：无需编程，5分钟完成
```

**方案B：Capacitor打包（推荐，更灵活）**

```bash
# 1. 安装Capacitor
npm install @capacitor/core @capacitor/cli
npm install @capacitor/android

# 2. 初始化Capacitor
npx cap init "勿蹉跎" "com.wucuoduo.app"

# 3. 复制Web资源
npx cap add android
npx cap copy android

# 4. 打开Android Studio
npx cap open android

# 5. 在Android Studio中：
#    - Build → Generate Signed Bundle/APK
#    - 选择APK
#    - 创建密钥（首次需要）
#    - 选择release
#    - Finish

# 6. APK位置：
android/app/build/outputs/apk/release/app-release.apk
```

**Capacitor配置文件 (capacitor.config.json)：**
```json
{
  "appId": "com.wucuoduo.app",
  "appName": "勿蹉跎",
  "webDir": ".",
  "server": {
    "androidScheme": "https"
  },
  "android": {
    "allowMixedContent": false,
    "backgroundColor": "#ffffff"
  }
}
```

**安装到手机：**
```
1. 将APK文件传到手机
2. 打开文件管理器
3. 点击APK文件
4. 允许安装未知来源应用
5. 安装完成！
```

---

### 📦 部署清单

**初次部署（一次性配置）：**
```
□ 注册Vercel账号（GitHub登录）
□ 安装Vercel CLI
□ 部署PWA到云端
□ 测试iOS/Android添加到主屏幕
□ 配置自动备份功能
□ （可选）打包APK备用
```

**日常使用：**
```
主要：通过主屏幕图标打开PWA
备份：每周手动导出一次数据
更新：修改代码后运行 vercel --prod
```

**更新流程：**
```bash
# 1. 修改代码
# 2. 部署新版本
vercel --prod

# 3. 用户端自动更新（PWA特性）
#    下次打开App时自动拉取最新版
```

---

## 📊 性能目标

```
启动时间：    < 1秒
首屏渲染：    < 800ms
交互响应：    < 100ms
内存占用：    < 30MB
安装包大小：  < 2MB
离线可用：    ✅
```

---

## 🎯 开发优先级

### 第一版必须有（3天完成）
1. ✅ 任务增删改查
2. ✅ 4种分类
3. ✅ 番茄钟25分钟
4. ✅ 今日完成数统计
5. ✅ 离线可用

### 已计划实现（第二阶段）
1. ✅ 摇一摇随机任务
2. ✅ 连续打卡成就系统
3. ✅ 专注模式 + 白噪音
4. ✅ 语音快速添加
5. ✅ 每日激励语 + 时间胶囊

### 未来可扩展（第三版）
1. Widget桌面小组件
2. 详细统计图表
3. 自定义番茄钟时长
4. 云同步（多设备）
5. 自定义主题皮肤
6. 社区激励排行榜

---

## 📝 总结

### 优势
- 🚀 **快速上线** - 3天MVP
- 💰 **成本低** - 复用现有代码
- 🌍 **跨平台** - iOS+Android
- 📦 **易安装** - 无需应用商店

### 下一步行动

**阶段1：开发核心功能（3天）**
1. ✅ Day 1 - 创建PWA项目结构 + 基础UI
2. ✅ Day 2 - 任务管理 + 番茄钟功能
3. ✅ Day 3 - 优化 + Service Worker

**阶段2：移动端特色功能（4天）**
1. ✅ Day 4 - 连续打卡成就 + 每日激励语
2. ✅ Day 5 - 摇一摇随机 + 语音添加
3. ✅ Day 6 - 专注模式 + 时间胶囊
4. ✅ Day 7 - 优化与真机测试

**阶段3：部署上线（0.5天）**
1. ✅ 部署到Vercel
2. ✅ 测试PWA安装（iOS + Android）
3. ✅ 配置数据备份功能

**阶段4：APK打包（可选，1天）**
1. ✅ 使用Capacitor打包
2. ✅ 生成release APK
3. ✅ 手机安装测试

---

**完整项目周期：7-9天**  
**MVP核心功能：3天**  
**增强功能：4天**  
**技术难度：⭐⭐⭐（中等偏上）**  
**成功概率：⭐⭐⭐⭐⭐（极高）**  
**部署方式：云端PWA + 本地APK双保险**

---

### 🎯 功能亮点总结

**核心功能（第一版）：**
- 任务管理（增删改查、分类筛选）
- 番茄钟计时器
- 今日统计
- 离线可用

**特色功能（第二版）：**
- 🎲 摇一摇随机任务 - 解决选择困难
- 🔥 连续打卡成就 - 游戏化激励
- 🎧 专注模式 + 白噪音 - 沉浸式体验
- 🎤 语音快速添加 - 解放双手
- ✨ 每日激励语 - 温暖的开始
- 📦 时间胶囊 - 每周成就回顾

准备好开始开发了吗？🚀
